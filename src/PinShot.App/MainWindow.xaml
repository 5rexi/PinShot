<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="PinShot.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="PinShot"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="PinShot">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <Grid Grid.Row="1" Padding="28" RowSpacing="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Spacing="6">
                <TextBlock
                    FontSize="30"
                    FontWeight="SemiBold"
                    Text="PinShot" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Use Win + Shift + S, then press F1 to pin the clipboard image." />
            </StackPanel>

            <Border
                Grid.Row="1"
                Padding="18"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="12">
                <Grid ColumnSpacing="14" RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="4">
                        <TextBlock FontWeight="SemiBold" Text="F1 pinning" />
                        <TextBlock
                            x:Name="StatusText"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                    <ToggleSwitch
                        x:Name="EnabledSwitch"
                        Grid.Column="1"
                        OffContent="Paused"
                        OnContent="Enabled"
                        Toggled="EnabledSwitch_Toggled" />
                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Spacing="4">
                        <TextBlock FontWeight="SemiBold" Text="Startup" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Launch PinShot when Windows starts." />
                    </StackPanel>
                    <ToggleSwitch
                        x:Name="AutoStartSwitch"
                        Grid.Row="1"
                        Grid.Column="1"
                        OffContent="Off"
                        OnContent="On"
                        Toggled="AutoStartSwitch_Toggled" />
                </Grid>
            </Border>

            <StackPanel Grid.Row="2" Spacing="8">
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Pinned image controls" />
                <TextBlock
                    Text="Drag to move. Mouse wheel zooms. Ctrl + wheel changes opacity. Ctrl + C copies. Double-click closes."
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
